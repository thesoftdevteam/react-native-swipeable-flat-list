/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { name as appName } from './app.json';
import ExampleApp from './src/App';

AppRegistry.registerComponent(appName, () => ExampleApp);
